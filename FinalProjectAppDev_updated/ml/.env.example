# Copy this file to .env in the same folder (ml/), then fill in YOUR OWN
# local values — must match whatever's in backend/.env.
# Never commit the real .env file — it's already in .gitignore.

DB_HOST=localhost
DB_PORT=3306
DB_USER=itern_app
DB_PASSWORD=changeme
DB_NAME=itern
